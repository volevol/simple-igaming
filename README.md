# 🧩 Slot Simulation 5x3 — Technical Reference

## 🔷 Paylines

**Paylines** — фиксированные маршруты на матрице слота (массив индексов строк для каждого барабана), по которым производится проверка выигрыша.

- используется классическая схема _left-to-right_;
- выигрыш засчитывается при совпадении **3+ одинаковых символов подряд с первого барабана**;
- каждая линия определяет свой путь через матрицу, например прямые, V-образные, перевёрнутые.

Paylines ограничивают пространство выигрышных траекторий и формируют традиционный формат расчёта выплат.

## 🔷 Ways

**243 Ways** — модель выплат без фиксированных линий, в которой выигрыш зависит не от формы линии (payline), а от количества совпадений на каждом барабане. Поле 5×3 даёт `3 × 3 × 3 × 3 × 3 = 243` возможных комбинации по барабанам.

- используется схема _left-to-right_;
- для каждого символа считается количество его попаданий (или Wild) на каждом барабане;
- комбинация продолжается до первого барабана без совпадений;
- выигрыш возможен при совпадении **на 3+ барабанах подряд**;
- итоговое число комбинаций (ways) — произведение количества совпадений по задействованным барабанам.

## 🔷 RTP (Return To Player)

**RTP** — математическое ожидание отдачи слота, выраженное в процентах. Определяется как отношение суммы выигрышей к сумме ставок на длинной серии вращений.

RTP = Σ(Win) / Σ(Bet)

- теоретическое значение основано на вероятностях комбинаций;
- фактическое значение приближается к теоретическому при больших выборках (100k–1M+ спинов);
- симуляция даёт оценку модели и используется для тестирования баланса выплат.

## 🔷 Stop-модель «one stop per reel + window»

Механика генерации результата базируется на принципе **одного stop-индекса на каждый барабан**.

1. выбирается один stop для каждого reel;
2. видимое окно формируется тремя соседними символами вокруг stop-позиции (top, middle, bottom);
3. выбранные элементы связаны порядком в reel-strip и не независимы.

Модель **не равна трём независимым stop-индексам**, поскольку при раздельной генерации вертикальная структура символов утрачивает связность, частотность перестаёт контролироваться через ленту, а распределение перестаёт соответствовать механической Reel-логике.

### 🔷 Wild (paylines)

**Wild** — универсальный заменяющий символ. Участвует в выигрышных линиях как эквивалент любого базового символа, кроме случаев, где предусмотрены исключения (например, Scatter и Bonus).  
При расчёте выигрышной комбинации Wild увеличивает длину совпадений слева направо и позволяет формировать более длинные line-wins.

Механизм определения линии с Wild:

- базовый символ определяется как **первый не-Wild слева**;
- Wild считается совпадающим с базовым символом;
- если линия состоит только из Wild — применяется выплата по Wild-payline.

Пример интерпретации:

| Линия             | Базовый символ | Комбинация | Выплата            |
| ----------------- | -------------- | ---------- | ------------------ |
| A • W • A • A • K | A              | 4-of-A     | по таблице A(4)    |
| W • W • K • K • Q | K              | 4-of-K     | по таблице K(4)    |
| W • W • W • W • W | Wild           | 5-of-Wild  | по таблице Wild(5) |

Частота Wild регулируется количеством его вхождений в **reel-strip**, что позволяет контролировать вероятность выпадения и силу потенциальных выплат.

### 🔷 Wild (ways)

243 Ways рассматривает **все возможные пути по барабанам**, поэтому Wild усиливает символы мультипликативно, а форма траектории не имеет значения — только количество совпавших символов на каждом барабане.

| Комбинация на барабанах | Базовый символ | Совпадения (per reel) | Ways | Выплата     |
| ----------------------- | -------------- | --------------------- | ---- | ----------- |
| A,W • A • W • A • —     | A              | 2 × 1 × 1 × 1         | 2    | A(4) × 2    |
| W,K • K • K,W • — • —   | K              | 2 × 1 × 2             | 4    | K(3) × 2    |
| W,W,W • W • W • W,W • W | Wild           | 3 × 1 × 1 × 2 × 1     | 6    | Wild(5) × 4 |

### 🔷 Scatter

Scatter — независимый от линий тип символа.  
Не образует комбинации через Paylines и не заменяется Wild'ом.  
Выплата рассчитывается по количеству Scatter-символов, присутствующих на экране целиком.

Принципы:

- Scatter оплачивается при наличии `≥ 3` на поле;
- не участвует в line-matching и не подменяется Wild-символом;
- Scatter-выплаты суммируются с line-win, не конфликтуя с ними;
- вероятность появления контролируется количеством Scatter в reel-strip.

Scatter применяется дополнительно к любым Payline-выигрышам на том же спине.

Если на экране больше Scatter, чем предусмотрено таблицей выплат,
значение автоматически клампится до максимального payout уровня.
